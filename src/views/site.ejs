<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Site Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex flex-column min-vh-100">

  <!-- 顶部导航 -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm px-4">
    <div class="container-fluid">
      <a class="navbar-brand d-flex align-items-center" href="#">
        <img src="/logo.png" alt="Logo" width="40" height="40" class="me-2" style="object-fit: cover;">
        <span class="fw-bold">Profitability Monitoring System</span>
      </a>
      <div class="d-flex">
        <a class="btn btn-outline-primary" href="/logout">Logout</a>
      </div>
    </div>
  </nav>
  
  <main class="flex-grow-1">
    <div class="container mt-5">

      <!-- 站点标题 -->
      <h2 class="mb-4 text-center">Site Details – <%= site.name %></h2>

      <!-- Network Hashrate & 更新时间 -->
      <div class="mb-3 text-center">
        <strong>Network Hashrate:</strong>
        <span class="text-primary"><%= networkHashrateTH %> TH/s</span>
        <div class="small text-muted">
          <em>Updated: <%= updatedTime %></em>
        </div>
      </div>

      <!-- 单站点数据表 -->
      <table class="table table-bordered table-hover table-striped align-middle text-center">
        <thead class="table-dark">
          <tr>
            <th>Site</th>
            <th># Machines</th>
            <th>Machine Type</th>
            <th>Power Cost ($/kWh)</th>
            <th>Kaspa Price (USD)</th>
            <th>Unit Profit ($)</th>
            <th>Coins Mined Daily</th>
            <th>Daily Revenue ($)</th>
            <th>Daily Power Cost ($)</th>
            <th>Daily Profit ($)</th>
            <th>Actions</th>  <!-- 新增 Actions 列 -->
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><%= site.name %></td>
            <td><%= site.num_machines %></td>
            <td><%= site.machine_type %></td>
            <td><%= site.power_rate %></td>
            <td><%= site.kas_price %></td>
            <td class="<%= parseFloat(site.unit_profit) >= 0 ? 'text-success' : 'text-danger' %>">
              <%= site.unit_profit %>
            </td>
            <td><%= site.daily_kas %></td>
            <td><%= site.revenue %></td>
            <td><%= site.cost %></td>
            <td class="<%= parseFloat(site.profit) >= 0 ? 'text-success' : 'text-danger' %>">
              <%= site.profit %>
            </td>
            <td>
              <!-- 将 Edit 按钮移到这里 -->
              <button class="btn btn-sm btn-primary"
                      data-bs-toggle="modal"
                      data-bs-target="#editSiteModal">
                Edit
              </button>
            </td>
          </tr>
        </tbody>
      </table>

    </div>
  </main>

  <!-- Edit Modal（保持不变） -->
  <div class="modal fade" id="editSiteModal" tabindex="-1" aria-labelledby="editSiteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <form method="POST" action="/site/<%= site.name.toLowerCase() %>/update">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="editSiteModalLabel">Edit <%= site.name %></h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
          </div>
          <div class="modal-body">
            <div class="mb-3">
              <label for="numMachines" class="form-label">Number of Machines</label>
              <input type="number" class="form-control" id="numMachines" name="num_machines"
                     value="<%= site.num_machines %>" required>
            </div>
            <div class="mb-3">
              <label for="powerRate" class="form-label">Power Cost ($/kWh)</label>
              <input type="number" step="0.0001" class="form-control" id="powerRate" name="power_rate"
                     value="<%= site.power_rate %>" required>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-primary">Save Changes</button>
          </div>
        </div>
      </form>
    </div>
  </div>

  <!-- 页脚 -->
  <footer class="bg-dark text-light text-center p-3 mt-5">
    <small>
      Data Source: <a href="https://api.kaspa.org" class="text-info">Kaspa.Org</a>
      <a href="https://api.minerstat.com" class="text-info">Minerstat</a><br>
      Contact: <a href="mailto:yuanfang@bginusa.com" class="text-info">yuanfang@bginusa.com</a>
    </small>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
